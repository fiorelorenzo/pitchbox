import type { ParsedEvent } from '../types.js';

/**
 * Parses one line of Codex CLI output into normalized events.
 * STUB — to be implemented when Codex runner lands.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function parseCodexLine(_line: string, _seqStart: number): ParsedEvent[] {
  return [];
}
