import type { ParsedEvent } from '../types.js';

/**
 * Parses one line of OpenCode CLI output into normalized events.
 * STUB — to be implemented when OpenCode runner lands.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function parseOpenCodeLine(_line: string, _seqStart: number): ParsedEvent[] {
  return [];
}
